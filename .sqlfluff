[sqlfluff]
dialect = bigquery

[sqlfluff:rules]
# Uncomment and adjust rules as needed
# max_line_length = 80

[sqlfluff:templater:jinja]
# Configuration for Jinja templating (if needed)
# apply_dbt_builtins = true