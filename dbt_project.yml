name: dbt_test
version: '1.0'

profile: default

model-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:         # directories to be removed by `dbt clean`
    - "target"
    - "dbt_packages"

models:
  dbt_test:
    +materialized: table
